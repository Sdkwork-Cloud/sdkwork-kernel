# SDKWork Agent App SDK Specs

This directory declares the component manifest for the app SDK family
`sdkwork-agent-app-sdk`.

The OpenAPI authority is `sdkwork-agent-app-api`, and generated transport
output is owned by
`sdkwork-agent-app-sdk-typescript/generated/server-openapi`.
