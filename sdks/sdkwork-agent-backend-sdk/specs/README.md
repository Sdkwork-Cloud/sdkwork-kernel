# SDKWork Agent Backend SDK Specs

This directory declares the component manifest for the backend SDK family
`sdkwork-agent-backend-sdk`.

The OpenAPI authority is `sdkwork-agent-backend-api`, and generated transport
output is owned by
`sdkwork-agent-backend-sdk-typescript/generated/server-openapi`.
