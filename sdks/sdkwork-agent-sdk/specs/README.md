# SDKWork Agent SDK Specs

This directory declares the component manifest for the developer/open SDK
family `sdkwork-agent-sdk`.

The OpenAPI authority is `sdkwork-agent-open-api`, and generated transport
output is owned by `sdkwork-agent-sdk-typescript/generated/server-openapi`.
