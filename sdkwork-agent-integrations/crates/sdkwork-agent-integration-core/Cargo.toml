[package]
name = "sdkwork-agent-integration-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "sdkwork_agent_integration_core"
path = "src/lib.rs"

[dependencies]
sdkwork-agent-kernel = { path = "../../../sdkwork-agent-kernel" }
