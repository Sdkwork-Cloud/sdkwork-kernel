[package]
name = "sdkwork-agent-integration-rig"
version = "0.1.0"
edition = "2021"

[lib]
name = "sdkwork_agent_integration_rig"
path = "src/lib.rs"

[dependencies]
sdkwork-agent-kernel = { path = "../../../sdkwork-agent-kernel" }
sdkwork-agent-integration-core = { path = "../sdkwork-agent-integration-core" }
