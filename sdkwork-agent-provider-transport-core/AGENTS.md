# SDKWork Agent SDK Backend Core

Read `../AGENTS.md` and `specs/component.spec.json` before editing this crate.

Verification:

```bash
cargo test --manifest-path sdkwork-agent-provider-transport-core/Cargo.toml
```
