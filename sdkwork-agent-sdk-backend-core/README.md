# SDKWork Agent SDK Backend Core

Backend host contracts for invoking external agent SDKs through Rust, Node/Bun,
Python, HTTP/OpenAPI, or IPC transports.

## Verification

```bash
cargo test --manifest-path sdkwork-agent-sdk-backend-core/Cargo.toml
```
