# SDKWork Agent SDK Backend IPC

JSON-RPC stdio transport and in-memory stubs for external agent SDK backend workers.

Verification:

```bash
cargo test --manifest-path sdkwork-agent-sdk-backend-ipc/Cargo.toml
```
