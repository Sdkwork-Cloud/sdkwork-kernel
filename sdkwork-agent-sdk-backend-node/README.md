# SDKWork Agent SDK Backend Node

Node/TypeScript JSON-RPC worker runtime for external agent SDK capabilities.

Verification:

```bash
cargo test --manifest-path sdkwork-agent-sdk-backend-node/Cargo.toml
```
