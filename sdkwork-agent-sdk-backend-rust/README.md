# SDKWork Agent SDK Backend Rust

In-process Rust runtime bridge that routes SDK capability invocations through kernel providers.

Verification:

```bash
cargo test --manifest-path sdkwork-agent-sdk-backend-rust/Cargo.toml
```
