[package]
name = "sdkwork-agent-streaming"
version = "0.1.0"
edition = "2021"
description = "SSE and WebSocket streaming protocol adapters for SDKWork kernel"

[dependencies]
sdkwork-agent-kernel.workspace = true
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
