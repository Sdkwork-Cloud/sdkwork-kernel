# SDKWork Agent Adapter Claude Code

Read `../../../AGENTS.md` and `specs/component.spec.json` before editing this crate.

Verification:

```bash
cargo test --manifest-path sdkwork-kernel-plugins/crates/sdkwork-agent-adapter-claude-code/Cargo.toml
```
