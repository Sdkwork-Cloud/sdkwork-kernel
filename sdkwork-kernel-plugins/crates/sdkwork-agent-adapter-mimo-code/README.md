# sdkwork-agent-adapter-mimo-code

External agent protocol adapter for the SDKWork agent kernel.

## Purpose

Maps an external agent runtime contract to SDKWork kernel SPI types through `sdkwork-agent-adapter-core` seams.

## Verification

```bash
cargo test --manifest-path sdkwork-kernel-plugins/crates/sdkwork-agent-adapter-mimo-code/Cargo.toml
```
