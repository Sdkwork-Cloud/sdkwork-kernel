# SDKWork Drive Kernel Plugin

Official SDKWork Kernel foundation plugin for Drive-backed storage providers.

This crate keeps the Agent Kernel core provider-neutral. It contributes Drive
storage provider manifests and typed wrappers over `sdkwork-drive-storage-contract`
without moving Drive ownership into the kernel core.
